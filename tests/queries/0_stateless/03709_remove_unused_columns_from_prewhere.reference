Expression (((Project names + Projection) + (WHERE + Change column names to column identifiers)))
Header: accountId Int64
        country String
  ReadFromMergeTree (default.test)
  Header: account_id Int64
          day DateTime
          source_type String
          country String
-----
Expression (((Project names + Projection) + (WHERE + Change column names to column identifiers)))
Header: accountId Int64
        country String
  ReadFromMergeTree (default.test)
  Header: account_id Int64
          country String
